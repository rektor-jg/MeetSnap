apiVersion: v1
kind: Service
metadata:
  name: {{ include "meetsnap.fullname" . }}
  labels:
    {{- include "meetsnap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meetsnap.selectorLabels" . | nindent 4 }}
