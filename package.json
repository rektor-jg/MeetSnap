{
  "name": "meetsnap",
  "version": "1.0.0",
  "description": "Aplikacja umożliwia nagrywanie lub wgrywanie plików audio, transkrypcję i (opcjonalnie) streszczenie, a następnie wyświetla TL;DR, Action items oraz Transkrypcję z możliwością eksportu.",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --define:process.env.API_KEY='\"$API_KEY\"' --loader:.tsx=tsx --minify"
  },
  "dependencies": {
    "@google/genai": "1.27.0",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5"
  }
}
